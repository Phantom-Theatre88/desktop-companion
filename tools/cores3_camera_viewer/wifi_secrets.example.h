#pragma once

// Copy this file to wifi_secrets.h on your Mac.
// wifi_secrets.h is ignored by Git and must never be committed.

constexpr char WIFI_SSID[] = "YOUR_WIFI_SSID";
constexpr char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";
